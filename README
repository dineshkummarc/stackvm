StackVM is a web-based virtual machine console and manager

To try:

1) start nodejs webstack:
    bin/webstack.js

2) start qemu and stackvm
    qemu -vnc :0.0 linux-0.2.img &

Then go to http://localhost:9000/
